# Application Name
spring.application.name=sample

# Active Profile (dev or prod)
# Default to 'dev' if not specified via environment variable
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Common configurations can be placed here
# Profile-specific configurations are in application-dev.properties and application-prod.properties